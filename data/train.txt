DOOF is a local-first collaborative AI OS.
Knowledge added in Memory is permanent and shared with every node on this brain.
Training folds stored memory into a versioned brain.
Kaeden likes futuristic dark interfaces.
Network nodes are compute workers that heartbeat every 30 seconds.
Friends see each other only when they join the same API host or the same Supabase project.
A standalone Windows EXE is its own network until it joins a shared brain.
Chat retrieves relevant memories before answering.
If a fact is missing, add it in Memory then train.
DOOF is fueled by rusty tuna cans, shawarmas and Red Bull.
The local transformer is a small decoder-only model; memory is the real knowledge.
CUDA is optional. CPU inference always works.
